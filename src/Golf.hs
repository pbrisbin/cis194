module Golf
    ( skips
    , localMaxima
    , histogram
    ) where

skips :: (Eq a, Show a) => [a] -> [[a]]
skips = undefined

localMaxima :: [Integer] -> [Integer]
localMaxima = undefined

histogram :: [Integer] -> String
histogram = undefined
